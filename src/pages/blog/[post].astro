---
export async function getStaticPaths() {
  return [
    { params: { post: "hello" } },
    { params: { post: "world" } },
  ];
}


let locale = Astro.url.pathname.slice(1, 3);

let title: string;
switch (locale) {
  case "es":
    title = "¡Hola Mundo!";
    break;
  case "fr":
    title = "Bonjour Monde!";
    break;
  default:
    title = "Hello World!";
    locale = "en"
}
---

<!DOCTYPE html>
<html lang={locale}>
<head>
  <meta charset="UTF-8" />
  <title>{title}</title>
</head>
<body>
  <h1>{locale} | {title}</h1>
</body>
</html>
